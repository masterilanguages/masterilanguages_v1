import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marklevine.languagemasteri',
  appName: 'Language Masteri',
  webDir: 'dist'
};

export default config;
