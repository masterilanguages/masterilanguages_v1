import PreviewShell from "@/components/landing/PreviewShell";

export const metadata = {
  title: "Masteri Languages — Preview (WebGL redesign)",
  description: "Living WebGL landing prototype for Masteri Languages.",
};

export default function PreviewLanding() {
  return <PreviewShell />;
}
