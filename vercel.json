{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "bash -c '[[ \"$VERCEL_GIT_COMMIT_REF\" == \"student-app\" ]] && exit 1 || exit 0'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
